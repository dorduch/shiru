# Yoto DIY - Flutter Readme

The application is implemented in `app/`. It uses:
- `riverpod` for state
- `just_audio` for audio
- `sqflite` for persistence
- A custom `PixelSprite` renderer for 16-bit animated graphics.

To run:
```sh
cd app
flutter run
```
# shiru
